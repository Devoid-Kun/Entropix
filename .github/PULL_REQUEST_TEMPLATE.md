## What changed

## Why

## Testing

- [ ] `cargo build --release` passes
- [ ] `cargo test` passes
- [ ] `cargo clippy --all-targets -- -D warnings` is clean
- [ ] `cargo sqlx prepare` was run if any queries changed

## Closes
